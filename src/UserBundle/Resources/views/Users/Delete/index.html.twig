{% extends 'base.html.twig' %}
{% block body %}
    <h3 class="text-center bg-grey"> User "{{ user_name }}" was removed!</h3>
    <div class="text-center">
        <a class="button rounded-btn submit-btn" href="{{ path( "user_homepage" ) }}">Main</a>
        <a class="button rounded-btn reload-btn" href="{{ path( "show_all_users" ) }}">Edit User</a>
    </div>
{% endblock %}