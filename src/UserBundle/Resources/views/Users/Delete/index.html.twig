{% extends 'base.html.twig' %}
{% block body %}
    <h3 class="text-center bg-grey"> User "{{ user_name }}" was removed!</h3>
    <div class="text-center">
        <a class="btn btn-info" href="{{ path( "user_homepage" ) }}">Main</a>
        <a class="btn btn-info" href="{{ path( "show_all_users" ) }}">Edit User</a>
    </div>
{% endblock %}